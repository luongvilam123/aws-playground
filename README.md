# aws-playground
# Note:
- which aws && ls -l $(which aws)
- aws --version
- source ~/.zshrc
- npx serverless deploy




## How to run:
- run "slsawsp" in command line to choose AWS Account
- run "cd src/stepFunctions/myDummyProject" go the the current project
- run "npx serverless package" in command line to build this project
- run "npm install"
- npx serverless deploy to deploy
